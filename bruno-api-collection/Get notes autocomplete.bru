meta {
  name: Get notes autocomplete
  type: http
  seq: 2
}

post {
  url: {{API}}/API/Expense/notes-autocomplete
  body: text
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:text {
  h
}
