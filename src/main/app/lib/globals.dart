library app.globals;

import 'dart:ui';

import 'package:app/service.dart';
import 'package:flutter/widgets.dart';

Service service = new Service();

// Colors
const Color backgroundColor = Color.fromRGBO(255, 255, 255, 1);
const Color accentColor = Color.fromRGBO(81, 173, 246, 1);

const BorderRadius defaultBorder = BorderRadius.all(Radius.circular(5));
const defaultPadding = 20.0;